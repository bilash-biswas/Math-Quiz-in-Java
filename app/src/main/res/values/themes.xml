<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MathQuiz" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.MathQuiz" parent="Base.Theme.MathQuiz" />

    <style name="ProgressBar" parent="Widget.Material3.CircularProgressIndicator">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/white</item>
         <item name="progressBarStyle">@color/white</item>
         <item name="android:progressBackgroundTintMode">@color/white</item>
    </style>
</resources>